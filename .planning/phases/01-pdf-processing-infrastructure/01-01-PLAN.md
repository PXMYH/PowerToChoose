---
phase: 01-pdf-processing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/config.py
  - api/routers/__init__.py
  - api/routers/plans.py
  - api/routers/efl.py
  - api/models/__init__.py
  - api/models/efl.py
  - api/models/job.py
  - api/main.py
  - api/pyproject.toml
autonomous: true
requirements:
  - PDF-01
  - PDF-02
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "FastAPI app uses router-based organization instead of monolithic main.py"
    - "Config loads settings from environment variables with sensible defaults"
    - "New dependencies are installed and importable"
    - "Existing /api/plans endpoint still works identically"
  artifacts:
    - path: "api/config.py"
      provides: "Centralized settings via pydantic-settings"
      contains: "class Settings"
    - path: "api/routers/plans.py"
      provides: "Existing plans endpoint extracted to router"
      exports: ["router"]
    - path: "api/routers/efl.py"
      provides: "EFL router stub for future endpoints"
      exports: ["router"]
    - path: "api/models/efl.py"
      provides: "Pydantic models for EFL processing"
      contains: "PDFType"
    - path: "api/models/job.py"
      provides: "Job status tracking models"
      contains: "JobStatus"
  key_links:
    - from: "api/main.py"
      to: "api/routers/plans.py"
      via: "app.include_router()"
      pattern: "include_router.*plans"
    - from: "api/config.py"
      to: ".env"
      via: "pydantic-settings env loading"
      pattern: "class Settings.*BaseSettings"
---

<objective>
Restructure the FastAPI project from a monolithic main.py into a router-based architecture, add configuration management, install new dependencies, and define the Pydantic models that all subsequent plans depend on.

Purpose: Every other plan in this phase imports from these modules. This is the foundation.
Output: Router-based FastAPI app, config module, Pydantic models for EFL and job tracking, new dependencies installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/main.py
@api/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create config module</name>
  <files>api/pyproject.toml, api/config.py</files>
  <action>
1. Add new dependencies to api/pyproject.toml:
   - pdfplumber>=0.11.9
   - aiosqlite>=0.21.0
   - tenacity>=9.0.0
   - pydantic-settings>=2.13.1
   - python-dotenv>=1.2.2
   - pytest>=9.0.2 (in dev dependencies via [project.optional-dependencies] test group)

2. Run `cd /Users/atlantis/workspace/power2choose/api && uv sync` to install.

3. Create api/config.py with a Settings class using pydantic-settings BaseSettings:
   - CACHE_DIR: str = "data/cache" (where downloaded PDFs are stored)
   - DATABASE_PATH: str = "data/power2choose.db" (SQLite DB path)
   - PDF_DOWNLOAD_TIMEOUT: int = 30 (seconds)
   - PDF_DOWNLOAD_MAX_RETRIES: int = 3
   - SCANNED_PDF_TEXT_THRESHOLD: int = 50 (chars below which PDF is classified as scanned)
   - PTC_API_URL: str = "https://www.powertochoose.org/en-us/service/v1/"
   - model_config with env_prefix="" and env_file=".env"
   - A module-level `settings = Settings()` singleton.
   - A `get_settings()` function for FastAPI dependency injection.
  </action>
  <verify>
    <automated>cd /Users/atlantis/workspace/power2choose/api && uv run python -c "from config import settings; print(settings.CACHE_DIR, settings.DATABASE_PATH)"</automated>
  </verify>
  <done>All new packages installed, config.py loads settings with defaults, importable from other modules.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor to routers and define Pydantic models</name>
  <files>api/routers/__init__.py, api/routers/plans.py, api/routers/efl.py, api/models/__init__.py, api/models/efl.py, api/models/job.py, api/main.py</files>
  <action>
1. Create api/routers/__init__.py (empty).

2. Create api/routers/plans.py:
   - Move the existing /api/plans endpoint from main.py into an APIRouter with prefix="/api".
   - Import PTC_URL from config (use settings.PTC_API_URL instead of hardcoded constant).
   - Keep exact same request/response behavior.

3. Create api/routers/efl.py:
   - Create an APIRouter with prefix="/api/efl".
   - Add a stub GET /api/efl/status/{job_id} endpoint that returns {"status": "not_implemented"} (placeholder for Plan 04).
   - Add a stub POST /api/efl/process endpoint that returns {"status": "not_implemented"} (placeholder for Plan 04).

4. Create api/models/__init__.py (empty).

5. Create api/models/efl.py with Pydantic models:
   - `PDFType(str, Enum)`: "text_based", "scanned", "unknown"
   - `DownloadResult(BaseModel)`: url (str), file_path (str), cached (bool), success (bool), error (str | None = None)
   - `PDFClassification(BaseModel)`: pdf_type (PDFType), text_length (int), file_path (str)
   - `TextExtractionResult(BaseModel)`: text (str), page_count (int), file_path (str), pdf_type (PDFType)

6. Create api/models/job.py with:
   - `JobStatus(str, Enum)`: "queued", "downloading", "extracting", "parsing", "storing", "completed", "failed"
   - `Job(BaseModel)`: id (str), plan_id (str), status (JobStatus), error (str | None = None), created_at (str), updated_at (str)

7. Rewrite api/main.py:
   - Keep FastAPI() creation and CORS middleware.
   - Remove the inline /api/plans endpoint.
   - Import and include plans_router and efl_router via app.include_router().
   - Add a startup event that creates data/cache directory (Path(settings.CACHE_DIR).mkdir(parents=True, exist_ok=True)).
  </action>
  <verify>
    <automated>cd /Users/atlantis/workspace/power2choose/api && uv run python -c "
from main import app
from fastapi.testclient import TestClient
client = TestClient(app)
# Verify plans endpoint still works (will call external API)
from models.efl import PDFType, DownloadResult, PDFClassification
from models.job import JobStatus, Job
print('PDFType values:', [e.value for e in PDFType])
print('JobStatus values:', [e.value for e in JobStatus])
# Verify EFL stub endpoints exist
resp = client.get('/api/efl/status/test-123')
print('EFL status stub:', resp.status_code, resp.json())
print('ALL IMPORTS OK')
"</automated>
  </verify>
  <done>
  - main.py uses include_router for plans and efl routers
  - /api/plans endpoint works identically to before
  - /api/efl/status/{job_id} returns stub response
  - All Pydantic models importable with correct fields and enums
  - data/cache directory created on startup
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/atlantis/workspace/power2choose/api && uv run uvicorn main:app --host 0.0.0.0 --port 8000` starts without errors
2. `curl http://localhost:8000/api/plans?zip_code=78665` returns plan data (existing behavior preserved)
3. `curl http://localhost:8000/api/efl/status/test` returns stub response
4. All new models importable: `uv run python -c "from models.efl import *; from models.job import *; from config import settings"`
</verification>

<success_criteria>
- Project restructured with routers, models, and config modules
- Existing /api/plans endpoint works unchanged
- New dependencies installed and importable
- Pydantic models define all types needed by subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-pdf-processing-infrastructure/01-01-SUMMARY.md`
</output>
